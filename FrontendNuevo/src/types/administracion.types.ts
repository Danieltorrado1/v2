// Admin entity types — backend endpoints pending for: empresas, contratos, cargos, requisitos, municipios.
// Mock shapes live in cg.types.ts; these will become the canonical types once endpoints exist.

export type EstadoAdministracion = 'activo' | 'inactivo';
