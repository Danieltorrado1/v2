import { useEffect, useMemo, useState } from 'react';
import { agendaApi } from '../../services/agendaApi';
import { useAuth } from '../../context/AuthContext';
import AgendaTaskEditForm from './components/AgendaTaskEditForm';
import AgendaTaskAssignForm from './components/AgendaTaskAssignForm';
import './AgendaOperativaPage.css';
type Task=Record<string,any>;type Detail=Task&{seguimientos:any[];asignaciones:any[];reprogramaciones:any[];participantes:any[]};const today=()=>new Date().toLocaleDateString('en-CA',{timeZone:'America/Bogota'});
export default function AgendaOperativaPage(){const {user}=useAuth();const can=(p:string)=>Boolean(user?.permissions.includes(p)||user?.permissions.includes('agenda.manage'));const [items,setItems]=useState<Task[]>([]),[summary,setSummary]=useState<any>(),[detail,setDetail]=useState<Detail|null>(null),[editing,setEditing]=useState(false),[assigning,setAssigning]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState(''),[notice,setNotice]=useState(''),[view,setView]=useState('Mi día'),[status,setStatus]=useState(''),[query,setQuery]=useState('');const load=async()=>{setLoading(true);try{const [l,s]=await Promise.all([agendaApi.list({page:1,limit:100,q:query||undefined,estado:status||undefined}),agendaApi.summary()]);setItems(l.items);setSummary(s);setError('');}catch(e){setError(e instanceof Error?e.message:'No fue posible cargar Agenda');}finally{setLoading(false);}};useEffect(()=>{void load();},[status]);const open=async(id:number)=>{try{setDetail(await agendaApi.get<Detail>(id));setEditing(false);setAssigning(false);}catch(e){setError(e instanceof Error?e.message:'No fue posible cargar el detalle');}};const after=async(task:Task)=>{setDetail(x=>x?{...x,...task}:x);setEditing(false);setAssigning(false);setNotice('Cambios guardados');await load();};const visible=useMemo(()=>view==='Mi día'?items.filter(t=>t.fecha_prevista===today()):items,[items,view]);return <main className="agenda-page"><header><div><span className="agenda-eyebrow">OPERACIÓN · AGENDA</span><h1>Agenda Operativa</h1></div></header>{error&&<div className="agenda-error" role="alert">{error}</div>}{notice&&<div className="agenda-notice" role="status">{notice}</div>}<section className="agenda-summary">{[['Pendientes',summary?.pendientes],['Para hoy',summary?.para_hoy],['Vencidas',summary?.vencidas],['Terminadas',summary?.terminadas]].map(x=><article key={String(x[0])}><span>{x[0]}</span><strong>{loading?'—':x[1]}</strong></article>)}</section><nav className="agenda-tabs">{['Mi día','Bandeja','Seguimientos','Semana'].map(v=><button className={view===v?'active':''} key={v} onClick={()=>setView(v)}>{v}</button>)}</nav><section className="agenda-filters"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar tareas"/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos</option><option value="PENDIENTE">Pendientes</option><option value="EN_PROCESO">En proceso</option><option value="TERMINADA">Terminadas</option></select><button onClick={()=>void load()}>Filtrar</button></section><section className="agenda-list agenda-panel">{loading&&<div className="agenda-empty">Cargando…</div>}{visible.map(t=><button className="agenda-task" key={t.id} onClick={()=>void open(t.id)}><span><strong>{t.titulo}</strong><small>{t.tipo} · {t.responsable_nombre}</small></span><em>{t.estado}</em></button>)}</section>{detail&&(editing?<div className="agenda-overlay"><AgendaTaskEditForm task={detail} onSaved={after} onCancel={()=>setEditing(false)}/></div>:assigning?<div className="agenda-overlay"><AgendaTaskAssignForm task={detail} onSaved={after} onCancel={()=>setAssigning(false)}/></div>:<div className="agenda-overlay"><aside className="agenda-drawer"><div className="agenda-drawer-toolbar"><button onClick={()=>setDetail(null)}>Cerrar</button>{can('agenda.update')&&<button onClick={()=>setEditing(true)}>Editar</button>}{can('agenda.assign')&&<button onClick={()=>setAssigning(true)}>Cambiar responsable</button>}</div><h2>{detail.titulo}</h2><p>{detail.descripcion||'Sin descripción'}</p><p>{detail.tipo} · {detail.prioridad} · {detail.estado}</p><p>Responsable: {detail.responsable_nombre}<br/>Creador: {detail.creador_nombre}<br/>Fecha: {detail.fecha_prevista}<br/>Límite: {detail.fecha_limite||'—'}</p><h3>Asignaciones</h3>{detail.asignaciones?.map(x=><p key={x.id}>{x.responsable_anterior_id||'—'} → {x.responsable_nuevo_id} · {x.motivo||''}</p>)}</aside></div>)}</main>}
